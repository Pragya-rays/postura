# Copy to frontend/.env.local. Read at build time (Next.js inlines
# NEXT_PUBLIC_* vars into the client bundle), so re-run `npm run build` /
# restart `npm run dev` after changing this.
NEXT_PUBLIC_API_URL=http://localhost:8000
